version: '3.4'

services:

  php-fpm:
    build:
      context: .
      target: bedrockapp_php
  nginx:
    build:
      context: .
      target: bedrockapp_nginx
    depends_on:
      - php-fpm
    ports:
      - "80:80"
